<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multicampus.topicsation.repository.ILoginDAO">
    <!--로그인-->
    <select id="login" resultType="com.multicampus.topicsation.dto.LoginDTO" parameterType="java.util.Map">
        select email,password, role from user where email = #{email}
    </select>
</mapper>